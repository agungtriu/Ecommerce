<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/tv_item_payment"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:drawablePadding="16dp"
        android:fontFamily="@font/poppins_500"
        android:padding="16dp"
        android:text="@string/payment_virtual_account_hint"
        android:textColor="?attr/colorOnSurfaceVariant"
        android:textSize="14sp"
        app:drawableEndCompat="@drawable/ic_arrow_forward_ios"
        app:drawableStartCompat="@drawable/ic_add_card"
        app:layout_constraintTop_toTopOf="parent" />

    <com.google.android.material.divider.MaterialDivider
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="16dp"
        app:layout_constraintTop_toBottomOf="@id/tv_item_payment" />

</androidx.constraintlayout.widget.ConstraintLayout>